--- app/page.tsx
+++ app/page.tsx
@@ -51,15 +51,11 @@
               data: p.data,
               messages: p.messages
             }));
             setProjects(mappedProjects);
-            
-            // 为了保持和之前一样的状态，如果服务端返回了项目，并且我们有一个活跃的 id，
-            // 确保相关对话也被加载过来，否则如果没有未保存的草稿，其实 zustand 本地已经有了
           }
         })
         .catch(console.error);
     }
-  }, [user, setProjects]);
+  }, [user]); // We do NOT put setProjects here to avoid infinite loops, but it comes from Zustand
 
   if (!isHydrated) return null; // Avoid hydration mismatch for Persist store
